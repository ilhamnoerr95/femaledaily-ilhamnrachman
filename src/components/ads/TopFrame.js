import { Fragment } from "react"
import style from "/src/styles/Ads.module.css"

const topFrame = ()=>{
    return <Fragment>
        <section>
            <div className={`${style.Box} ${style.TopFrame}`}>
                    Top Frame 970x50 
            </div>
        </section>

    </Fragment> 
    
}

export default topFrame